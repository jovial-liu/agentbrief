# Contributing

1. Fork the repository and create a focused branch.
2. Run `npm test` and `npm run lint` before opening a pull request.
3. Add a regression test for behavior changes.
4. Keep the CLI dependency-free and avoid sending workspace data over the network.

Please include a short before/after example in the pull request description.
